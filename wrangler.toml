name = "daily-grid-hq"
compatibility_date = "2025-07-18"
pages_build_output_dir = "./public"

# D1 database binding - configure via Cloudflare dashboard or local override
# For local dev: wrangler pages dev . --d1 DB=daily-grid-hq-db
# For production: Set binding in Cloudflare Pages dashboard -> Settings -> Functions -> D1
[[d1_databases]]
binding = "DB"
database_name = "daily-grid-hq-db"
database_id = "REPLACE_ME_FOR_LOCAL_DEV"
