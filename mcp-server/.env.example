# Daily Grid HQ MCP Server Configuration

# Required: URL of your deployed kanban board
KANBAN_API_URL=http://localhost:8796

# Optional: API token for authentication (if using Bearer token auth)
# Leave empty if using Cloudflare Access or dev mode
KANBAN_API_TOKEN=
